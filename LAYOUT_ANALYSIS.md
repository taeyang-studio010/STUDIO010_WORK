# 아래 4개 박스가 좁아지는 원인 분석

## 1. 레이아웃 구조

```
main (flex, pl-[240px] = 사이드바 공간)
└── div (flex flex-1 min-h-0)  ← 가로: [콘텐츠 영역 | ActivityFeed 300px]
    ├── 콘텐츠 div (flex-1 min-w-0 p-6 lg:p-8)  ← 남은 너비 전부
    │   ├── KPI 1x3
    │   ├── 1/4/10 (w-full)
    │   └── 2x2 그리드 (w-full max-w-full)
    └── ActivityFeed (w-[300px] shrink-0)
```

## 2. 좁아지는 원인

### (1) **콘텐츠 영역이 ActivityFeed와 나란히 있음**
- **실제 사용 가로 폭** = 뷰포트 − 사이드바(240px) − **액티비티 피드(300px)**.
- 그래서 1/4/10과 2x2가 들어가는 콘텐츠 영역 자체가 이미 줄어든 상태입니다.
- 2x2가 “좁다”고 느껴지는 이유 중 하나는, **전체 화면이 아니라 이렇게 줄어든 영역 안**에 있기 때문입니다.

### (2) **flex + min-w-0으로 인한 추가 축소**
- 콘텐츠 div에 `min-w-0`이 있어서, flex 레이아웃에서 **필요 시 콘텐츠 영역이 더 줄어들 수 있습니다.**
- 2x2 그리드도 이 div의 자식이라, **부모가 줄어들면 2x2도 같이 좁아집니다.**

### (3) **1/4/10과 2x2가 “다르게 보일” 수 있는 이유**
- 1/4/10은 카드 3개가 가로로 넓게 퍼져 있어서 “넓어 보이고”,
- 2x2는 4개 박스가 2열로 나뉘어 있어서 **같은 콘텐츠 폭의 절반(50%)씩** 쓰게 됩니다.
- 그래서 **실제 픽셀 폭**은 2x2의 한 칸이 1/4/10 한 칸보다 훨씬 좁게 느껴질 수 있습니다.
- 추가로, 그리드 자체가 부모보다 좁게 계산되는 경우(예: flex 자식의 min-width 동작)가 있으면, 2x2만 더 좁아져 보일 수 있습니다.

### (4) **좌/우 열이 다르게 보이는 경우**
- 그리드는 `gridTemplateColumns: "1fr 1fr"`로 **이론상 50:50**이지만,
- 내부 콘텐츠(긴 텍스트, 버튼 등)나 스크롤/overflow 설정 때문에 **한쪽 열만 더 좁아 보이거나** 텍스트가 더 많이 줄바꿈될 수 있습니다.

## 3. 대응 방향

1. **2x2가 1/4/10과 같은 너비를 쓰도록 보장**  
   - 1/4/10과 2x2를 같은 “전체 너비” 블록 안에 두고, 2x2에 `width: 100%` + `min-width: 0`을 명시해, **항상 상단 블록과 동일한 폭**을 쓰게 합니다.

2. **콘텐츠 영역이 너무 좁아지지 않게**  
   - 콘텐츠 div에 `min-width`를 두어, 액티비티 피드와 나란히 있을 때도 2x2가 **일정 폭 이하로는 줄어들지 않게** 할 수 있습니다.

3. **그리드 열 비율 고정**  
   - `grid-template-columns: 1fr 1fr`을 유지하고, 각 셀에 `min-width: 0`을 주어 **그리드가 부모보다 좁게 계산되지 않도록** 합니다.

위 수정을 적용하면 “아래 4개 박스가 자꾸 좁아지는” 현상을 줄일 수 있습니다.
